## Faceless Viral Studio

Generate faceless YouTube video blueprints engineered for virality. Drop in your niche, goals, and production resources to receive:

- Channel positioning + programming pillars
- Viral packaging angles and hook formulas
- Three cinematic hero video concepts with story beats, B-roll prompts, and CTAs
- Retention + editing stack recommendations
- Automation tools, faceless execution tactics, and distribution flywheels
- Launch checklist and bonus growth plays

Built with [Next.js 14](https://nextjs.org/) and designed for painless deployment on Vercel.

## 🚀 Quick start

```bash
npm install
npm run dev
```

Visit [http://localhost:3000](http://localhost:3000) to remix the strategy in real time.

## 🧱 Tech

- Next.js App Router + TypeScript
- CSS Modules for a cinematic dashboard aesthetic
- Deterministic strategy generator powered by curated playbooks

## 🛠️ Scripts

- `npm run dev` – start the dev server
- `npm run build` – production build
- `npm run start` – run the production build locally
- `npm run lint` – lint the project

## 📦 Deployment

The project is Vercel-ready. Build before deploying:

```bash
npm run build
```

Then deploy with the Vercel CLI:

```bash
vercel deploy --prod --yes --token $VERCEL_TOKEN --name agentic-ec965f6d
```
